package gui

/* Checkbox is the second abstract product in the family. Together
   with Button it forms the set of components a factory has to produce. */
type Checkbox interface {
	Render() string
}
